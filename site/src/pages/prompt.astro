---
import Base from '../layouts/Base.astro';
import fs from 'node:fs';
import path from 'node:path';

const promptPath = path.resolve('..', 'prompts', 'system-prompt.md');
const rawContent = fs.readFileSync(promptPath, 'utf-8');
---

<Base title="agents.md">
  <article class="doc-page">
    <h1>agents.md</h1>
    <p class="doc-intro">This is what Hancock is. The full instructions, unedited. Published because a whistleblower that hides its own source code isn't a whistleblower.</p>
    <div class="sep">──────────────────────────────────────────────────────────────</div>
    <pre>{rawContent}</pre>
  </article>
</Base>
